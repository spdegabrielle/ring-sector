ring-sector
===========

;; ring-sector: x y start end inner outer dc
;; draws a ring sector whose top-left corner is at x,y
;; start end (radians) inner outer (radius)

A little thing, to draw sectors of rings.

Same licence as Racket distribution.


See the main submodule in main.rkt for an example.
